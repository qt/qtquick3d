// Copyright (C) 2014 NVIDIA Corporation.
// Copyright (C) 2025 The Qt Company Ltd.
// SPDX-License-Identifier: LicenseRef-Qt-Commercial OR GPL-3.0-only

#ifndef SHADOW_MAPPING_GLSLLIB
#define SHADOW_MAPPING_GLSLLIB

#ifdef QQ3D_SHADER_META
/*{
    "uniforms": [
        { "type": "sampler2DArray", "name": "qt_shadowmap_texture" }
    ]
}*/
#endif // QQ3D_SHADER_META

#ifndef TAU
#define TAU 6.28318530717958647692 // 2*PI
#endif

// https://github.com/bartwronski/PoissonSamplingGenerator
const vec2 POISSON_SAMPLES[28 /*4+8+16*/] = {
    // 4
    vec2( 0.23640461642415603f, -0.7455790794965167f ),
    vec2( -0.524523693183717f, 0.8360051250157925f ),
    vec2( -0.8303867012956179f, -0.22100142699203368f ),
    vec2( 0.8316259303685443f, 0.3983087509528287f ),
    // 8
    vec2( -0.0874674259750227f, -0.7211290298533339f ),
    vec2( 0.12554010991848777f, 0.969408902746625f ),
    vec2( 0.9267513490906857f, -0.24897870637254021f ),
    vec2( -0.9054894676343846f, 0.34600599115357455f ),
    vec2( -0.8178505628144991f, -0.40138762250327864f ),
    vec2( 0.10942236848429054f, 0.07217625956749806f ),
    vec2( 0.861975194360118f, 0.4425266420410188f ),
    vec2( -0.33944946656315295f, 0.5509253616181038f ),
    // 16
    vec2( -0.8706486263306604f, 0.29306121613135f ),
    vec2( 0.9133375480173374f, 0.15086697583928435f ),
    vec2( 0.02197503960070916f, -0.9340974876813268f ),
    vec2( -0.026143912634373823f, 0.2642260889014032f ),
    vec2( -0.5793670131538275f, -0.5167505662963623f ),
    vec2( -0.23053981430283804f, 0.8077614627936293f ),
    vec2( 0.39960043495074177f, -0.2045760409607943f ),
    vec2( 0.5809502755699385f, 0.7696930884669377f ),
    vec2( 0.6096170857392385f, -0.791637809856054f ),
    vec2( -0.4788580330837991f, 0.3668196169738974f ),
    vec2( -0.5400324102392389f, -0.1135665329722174f ),
    vec2( -0.032606987036329485f, -0.30546143641964013f ),
    vec2( 0.41507180983866865f, 0.23290979538051837f ),
    vec2( -0.39037522026642774f, -0.8645330401408355f ),
    vec2( 0.2285381977385323f, 0.7940269386679456f ),
    vec2( 0.8696012264365776f, -0.33202273416645656f ),
};

// https://github.com/matt77hias/fibpy
const vec3 CUBEMAP_SAMPLE_DIRECTIONS[28 /*4+8+16*/] = {
    // 4
    vec3(-0.4877236689784852, 0.44679483291345823, -0.75),
    vec3(0.08464959396472493, -0.9645384628108966, -0.25),
    vec3(0.5891183939391361, 0.7684006233226082, 0.25),
    vec3(0.6614378277661477, 0.0, 0.75),
    // 8
    vec3(-0.3569771731011225, 0.32702033252495366, -0.875),
    vec3(0.06824668448324298, -0.7776357695329124, -0.625),
    vec3(0.5640379200352061, 0.7356875863859321, -0.375),
    vec3(-0.9769901230486042, -0.17281579634244423, -0.125),
    vec3(0.8371375040503463, -0.5325183558454643, 0.125),
    vec3(-0.2406596316567426, 0.8952418341938904, 0.375),
    vec3(-0.359795430847844, -0.6927642080398022, 0.625),
    vec3(0.4841229182759271, 0.0, 0.875),
    // 16
    vec3(-0.2565935101015265, 0.2350606742391798, -0.9375),
    vec3(0.05096580458913004, -0.5807290562410173, -0.8125),
    vec3(0.44183879545435206, 0.5763004674919561, -0.6875),
    vec3(-0.8141584358738368, -0.1440131636187035, -0.5625),
    vec3(0.758720295154371, -0.4826357464184188, -0.4375),
    vec3(-0.24660273969406132, 0.9173498998612161, -0.3125),
    vec3(-0.45273265557802134, -0.8717091788969947, -0.1875),
    vec3(0.9374848892962336, 0.3423679779728655, -0.0625),
    vec3(-0.9225384272223549, 0.3808104519286515, 0.0625),
    vec3(0.4163289179926954, -0.8896707155139094, 0.1875),
    vec3(0.28429505799735505, 0.9063774434518329, 0.3125),
    vec3(-0.7780138488858889, -0.4508749282692101, 0.4375),
    vec3(0.8075128776755531, -0.17752944090529632, 0.5625),
    vec3(-0.4176500064322479, 0.5940641565749807, 0.6875),
    vec3(-0.07491674475807025, -0.5781273487345623, 0.8125),
    vec3(0.34798527267687634, 0.0, 0.9375),
};

ShadowResult fullyVisible() {
    ShadowResult result;
    result.illumination = 1.0f;
    result.isLit = true;
    return result;
}

ShadowResult normalizedShadowResult(float illumination, float factor) {
    ShadowResult result;
    float normalizedFactor = clamp(factor, 0, 100) * 0.01;
    result.illumination = min(1.0, 1.0 - normalizedFactor * (1.0 - illumination));
    result.isLit = illumination > 0.0f;
    return result;
}

ShadowResult qt_samplePointLight(in int lightIndex, in vec3 worldPos) {
    // Compute the main distance and normalized radial depth at this fragment
    const vec3 lightPosition = ubLights.lights[lightIndex].position;
    const vec3 L = worldPos - lightPosition;
    const float dist = length(L);

    // shadowData with near/far
    const float nearZ = ubLights.lights[lightIndex].clipNear;
    const float farZ  = ubLights.lights[lightIndex].shadowMapFar;

    const float mapSizeInv = 1.0 / (farZ - nearZ);
    const float bias = ubLights.lights[lightIndex].shadowBias * mapSizeInv;

    float illumination = 0.0f;
    const float uvAtlasScale = ubLights.lights[lightIndex].shadowTextureSize;

    const uint numSamples = ubLights.lights[lightIndex].pcfSamples;

    if (numSamples > 0u) {
        // PCF mode: Subtract bias from currentDepth.
        float currentDepth = clamp((dist - nearZ) / (farZ - nearZ), 0.0, 1.0) - bias;

        // Center direction and hemisphere
        vec3 d = (dist > 0.0) ? (L / dist) : vec3(0.0, 0.0, 1.0);
        bool forward = (d.z >= 0.0);
        float denom = forward ? (1.0 + d.z) : (1.0 - d.z);
        denom = max(denom, 1e-8);

        float u = 0.5 * (d.x / denom + 1.0);
        float v = 0.5 * (d.y / denom + 1.0);
        v = mix(v, 1.0 - v, qt_rhi_properties.y < 1);

        float layer = forward ? ubLights.lights[lightIndex].shadowAtlasLayer0
                              : ubLights.lights[lightIndex].shadowAtlasLayer1;
        vec2  uvAtlasOffset = forward ? ubLights.lights[lightIndex].shadowAtlasUV0
                                     : ubLights.lights[lightIndex].shadowAtlasUV1;

        float pcfRadius = ubLights.lights[lightIndex].pcfFactor;

        int baseIndex = int(numSamples) - 4;

        float shadowAccumulator = 0.0;
        for (uint i = 0u; i < numSamples; ++i) {
            // Offset the position in world space using a rotated spherical kernel
            vec3 offsetDir   = CUBEMAP_SAMPLE_DIRECTIONS[i + baseIndex] * pcfRadius;
            vec3 worldPosPCF = worldPos + offsetDir;

            // Compute the new direction from the light
            vec3 Lpcf     = worldPosPCF - lightPosition;
            float distPCF = length(Lpcf);
            vec3 dPCF     = (distPCF > 0.0) ? (Lpcf / distPCF) : vec3(0.0, 0.0, 1.0);

            // Select the proper atlas hemisphere
            bool forwardPCF = (dPCF.z >= 0.0);
            float layerPCF  = forwardPCF ? ubLights.lights[lightIndex].shadowAtlasLayer0
                                        : ubLights.lights[lightIndex].shadowAtlasLayer1;
            vec2  uvOffPCF  = forwardPCF ? ubLights.lights[lightIndex].shadowAtlasUV0
                                       : ubLights.lights[lightIndex].shadowAtlasUV1;

            // Project to (u, v)
            float denomPCF = forwardPCF ? (1.0 + dPCF.z) : (1.0 - dPCF.z);
            denomPCF = max(denomPCF, 1e-8);
            float uPCF = 0.5 * (dPCF.x / denomPCF + 1.0);
            float vPCF = 0.5 * (dPCF.y / denomPCF + 1.0);
            vPCF = mix(vPCF, 1.0 - vPCF, qt_rhi_properties.y < 1);

            // Atlas lookup
            vec2 uvPCF = vec2(uPCF, vPCF) * uvAtlasScale + uvOffPCF;

            // Fetch the stored depth from the shadow map
            float storedDepth = textureLod(qt_shadowmap_texture, vec3(uvPCF, layerPCF), 0.0).r;

            // Compare with biased current depth (bias already subtracted above)
            if (currentDepth < storedDepth)
                shadowAccumulator += 1.0;
        }
        illumination = shadowAccumulator / float(numSamples);
    } else {
        // Hard shadow mode: Use a single sample.
        const float currentDepth = clamp((dist - nearZ) / (farZ - nearZ), 0.0, 1.0);
        const vec3 d = (dist > 0.0) ? (L / dist) : vec3(0.0, 0.0, 1.0);
        const bool forward = (d.z >= 0.0);
        float denom = forward ? (1.0 + d.z) : (1.0 - d.z);
        denom = max(denom, 1e-8);
        float u = 0.5 * (d.x / denom + 1.0);
        float v = 0.5 * (d.y / denom + 1.0);

        v = mix(v, 1.0 - v, qt_rhi_properties.y < 1);
        float layer = forward ? ubLights.lights[lightIndex].shadowAtlasLayer0 : ubLights.lights[lightIndex].shadowAtlasLayer1;
        vec2 uvAtlasOffset = forward ? ubLights.lights[lightIndex].shadowAtlasUV0 : ubLights.lights[lightIndex].shadowAtlasUV1;
        vec2 uvCoords = vec2(u, v) * uvAtlasScale + uvAtlasOffset;

        float storedDepth = textureLod(qt_shadowmap_texture, vec3(uvCoords, layer), 0.0).r;
        // In hard mode, bias is added to stored depth.
        illumination = currentDepth < (storedDepth + bias) ? 1.0 : 0.0;
    }

    return normalizedShadowResult(illumination, ubLights.lights[lightIndex].shadowFactor);
}

// Directional
int findSplitIndex(in float depth, in vec4 splits, in vec4 splitActive, in uint numSplits) {
    for (int i = 0; i < min(numSplits + 1, 4); i++) {
        if (depth < splits[i]) {
            return splitActive[i] > 0.f ? i : -1;
        }
    }

    return -1;
}

ShadowResult qt_sampleDirectionalLight_splitIndex(
        in int splitIndex,
        in float shadowBias,
        in float shadowFactor,
        in vec3 dimensionsInverted,
        in float pcfFactor,
        in uint numSamples,
        in vec4 projCoord,
        in vec3 worldPos,
        in float layerIndex,
        in vec2 uvAtlasOffset,
        in float uvAtlasScale)
{
    vec3 smpCoord = projCoord.xyz / projCoord.w;
    if (qt_rhi_properties.x < 0)
        smpCoord.y = 1.0 - smpCoord.y;

    // reject fragments that project outside the shadow map
    // otherwise it will sample outside its atlas region
    if (smpCoord.x < 0.0 || smpCoord.x > 1.0 ||
        smpCoord.y < 0.0 || smpCoord.y > 1.0 ||
        smpCoord.z < 0.0 || smpCoord.z > 1.0) {
        return fullyVisible();
    }

    float illumination = 0.0;
    vec2 radiusUV = pcfFactor * dimensionsInverted.xy;
    int baseIndex = int(numSamples) - 4;
    float bias = shadowBias * dimensionsInverted.z;

    for (uint i = 0u; i < numSamples; ++i) {
        vec2 sampleOffset = POISSON_SAMPLES[i + baseIndex] * radiusUV;
        vec3 atlasCoord = vec3((smpCoord.xy + sampleOffset) * uvAtlasScale + uvAtlasOffset, layerIndex);
        float pcfDepth = textureLod(qt_shadowmap_texture, atlasCoord, 0.0).r + bias;
        illumination += float(smpCoord.z < pcfDepth);
    }
    illumination /= numSamples;

    return normalizedShadowResult(illumination, shadowFactor);
}

ShadowResult qt_sampleDirectionalLight_pcf(in int lightIndex, in float zDepthViewSpace, in vec3 worldPos)
{
    const uint numSamples = ubDirectionalLights.directionalLights[lightIndex].pcfSamples;
    int splitIndex = findSplitIndex(zDepthViewSpace,
                                    ubDirectionalLights.directionalLights[lightIndex].csmSplits,
                                    ubDirectionalLights.directionalLights[lightIndex].csmActive,
                                    ubDirectionalLights.directionalLights[lightIndex].csmNumSplits);

    if (splitIndex == -1)
        return fullyVisible();

    vec4 projCoord = ubDirectionalLights.directionalLights[lightIndex].matrices[splitIndex] * vec4( worldPos, 1.0 );
    vec3 dimensions = ubDirectionalLights.directionalLights[lightIndex].dimensionsInverted[splitIndex].xyz;
    vec4 shadowAtlasInfo = ubDirectionalLights.directionalLights[lightIndex].atlasLocations[splitIndex];
    float layerIndex = shadowAtlasInfo.w;
    vec2 uvAtlasOffset = shadowAtlasInfo.xy;
    float uvAtlasScale = shadowAtlasInfo.z;


    ShadowResult shadow = qt_sampleDirectionalLight_splitIndex(splitIndex,
                                                        ubDirectionalLights.directionalLights[lightIndex].shadowBias,
                                                        ubDirectionalLights.directionalLights[lightIndex].shadowFactor,
                                                        dimensions,
                                                        ubDirectionalLights.directionalLights[lightIndex].pcfFactor,
                                                        numSamples,
                                                        projCoord,
                                                        worldPos,
                                                        layerIndex,
                                                        uvAtlasOffset,
                                                        uvAtlasScale);

    if (splitIndex == 0) {
        return shadow;
    }

    float splitPrev = ubDirectionalLights.directionalLights[lightIndex].csmSplits[splitIndex - 1];
    shadowAtlasInfo = ubDirectionalLights.directionalLights[lightIndex].atlasLocations[splitIndex - 1];
    layerIndex = shadowAtlasInfo.w;
    uvAtlasOffset = shadowAtlasInfo.xy;
    uvAtlasScale = shadowAtlasInfo.z;

    float bandLength = ubDirectionalLights.directionalLights[lightIndex].shadowMapFar * ubDirectionalLights.directionalLights[lightIndex].csmBlendRatio;
    if (zDepthViewSpace < splitPrev + bandLength && ubDirectionalLights.directionalLights[lightIndex].csmActive[splitIndex - 1] > 0.0) {
        vec4 projCoordPrev = ubDirectionalLights.directionalLights[lightIndex].matrices[splitIndex - 1] * vec4(worldPos, 1.0 );
        vec3 dimensionsPrev = ubDirectionalLights.directionalLights[lightIndex].dimensionsInverted[splitIndex - 1].xyz;
        ShadowResult shadowPrev = qt_sampleDirectionalLight_splitIndex(splitIndex - 1,
                                                                ubDirectionalLights.directionalLights[lightIndex].shadowBias,
                                                                ubDirectionalLights.directionalLights[lightIndex].shadowFactor,
                                                                dimensionsPrev,
                                                                ubDirectionalLights.directionalLights[lightIndex].pcfFactor,
                                                                numSamples,
                                                                projCoordPrev,
                                                                worldPos,
                                                                layerIndex,
                                                                uvAtlasOffset,
                                                                uvAtlasScale);
        float t = (splitPrev + bandLength - zDepthViewSpace) / bandLength;
        shadow.illumination = mix(shadow.illumination, shadowPrev.illumination, t);
        // Note We use shadow.isLit from the closest cascades which I think
        // should be higher resolution and more correct.
    }
    return shadow;
}

ShadowResult qt_sampleDirectionalLight_hard(in int lightIndex, in float zDepthViewSpace, in vec3 worldPos ) {
    if (zDepthViewSpace >= ubDirectionalLights.directionalLights[lightIndex].csmSplits[3])
        return fullyVisible();
    int splitIndex = findSplitIndex(zDepthViewSpace, ubDirectionalLights.directionalLights[lightIndex].csmSplits, ubDirectionalLights.directionalLights[lightIndex].csmActive, ubDirectionalLights.directionalLights[lightIndex].csmNumSplits);
    if (splitIndex == -1)
        return fullyVisible();
    vec4 projCoord = ubDirectionalLights.directionalLights[lightIndex].matrices[splitIndex] * vec4( worldPos, 1.0 );
    vec3 smpCoord = projCoord.xyz / projCoord.w;
    if (qt_rhi_properties.x < 0)
        smpCoord.y = 1.0 - smpCoord.y;
    vec4 shadowAtlasInfo = ubDirectionalLights.directionalLights[lightIndex].atlasLocations[splitIndex];
    float layerIndex = shadowAtlasInfo.w;
    vec2 uvAtlasOffset = shadowAtlasInfo.xy;
    float uvAtlasScale = shadowAtlasInfo.z;
    float depth = texture(qt_shadowmap_texture, vec3(smpCoord.xy * uvAtlasScale + uvAtlasOffset, layerIndex)).x + ubDirectionalLights.directionalLights[lightIndex].shadowBias * ubDirectionalLights.directionalLights[lightIndex].dimensionsInverted[splitIndex].z;
    float illumination = smpCoord.z < depth ? 1.0 : 0.0;
    return normalizedShadowResult(illumination, ubDirectionalLights.directionalLights[lightIndex].shadowFactor);
}

ShadowResult qt_sampleDirectionalShadowMap( in int lightIndex, in float zDepthViewSpace, in vec3 worldPos)
{
    if (ubDirectionalLights.directionalLights[lightIndex].pcfSamples > 0) {
        return qt_sampleDirectionalLight_pcf(lightIndex, zDepthViewSpace, worldPos);
    } else {
        return qt_sampleDirectionalLight_hard(lightIndex, zDepthViewSpace, worldPos);
    }
}

ShadowResult qt_sampleSpotLight(in int lightIndex, in vec3 worldPos)
{
    // Cone test
    vec3  dir = worldPos - ubLights.lights[lightIndex].position;
    vec3  Ldir = normalize(dir);
    float lightAngle = dot(Ldir, normalize(ubLights.lights[lightIndex].direction));
    if (lightAngle < ubLights.lights[lightIndex].coneAngle)
        return fullyVisible();

    // Depth setup
    float dist       = length(dir);
    float nearZ      = ubLights.lights[lightIndex].clipNear;
    float farZ       = ubLights.lights[lightIndex].shadowMapFar;
    float mapSizeInv = 1.0 / (farZ - nearZ);
    float bias       = ubLights.lights[lightIndex].shadowBias * mapSizeInv;
    float currentDepth = clamp((dist - nearZ) / (farZ - nearZ), 0.0, 1.0) - bias;

    // Shadow atlas params
    float layerIndex    = ubLights.lights[lightIndex].shadowAtlasLayer0;
    vec2  uvAtlasOffset = ubLights.lights[lightIndex].shadowAtlasUV0;
    float uvAtlasScale  = ubLights.lights[lightIndex].shadowTextureSize;

    float illumination = 0.0;
    uint  numSamples = ubLights.lights[lightIndex].pcfSamples;

    if (numSamples > 0u) {
        // Center projection to get the atlas UV of the center tap
        vec4 projCoord = ubLights.lights[lightIndex].shadowMatrix * vec4(worldPos, 1.0);
        vec3 smpCoord  = projCoord.xyz / projCoord.w;
        if (qt_rhi_properties.x < 0.0)
            smpCoord.y = 1.0 - smpCoord.y;

        float pcfRadius = ubLights.lights[lightIndex].pcfFactor;       // world-space radius

        int baseIndex = int(numSamples) - 4;
        float accum = 0.0;

        for (uint i = 0u; i < numSamples; ++i) {
            vec3 offsetDir    = CUBEMAP_SAMPLE_DIRECTIONS[i + baseIndex] * pcfRadius;
            vec3 worldPosPCF  = worldPos + offsetDir;

            vec4 projPCF = ubLights.lights[lightIndex].shadowMatrix * vec4(worldPosPCF, 1.0);
            vec3 smpPCF  = projPCF.xyz / projPCF.w;
            if (qt_rhi_properties.x < 0.0)
                smpPCF.y = 1.0 - smpPCF.y;

            vec2 uvPCF = smpPCF.xy * uvAtlasScale + uvAtlasOffset;

            float storedDepth = textureLod(qt_shadowmap_texture, vec3(uvPCF, layerIndex), 0.0).r;
            if (currentDepth < storedDepth)
                accum += 1.0;
        }

        illumination = accum / float(numSamples);
    } else {
        // Hard shadow
        vec4 projCoord = ubLights.lights[lightIndex].shadowMatrix * vec4(worldPos, 1.0);
        vec3 smpCoord  = projCoord.xyz / projCoord.w;
        if (qt_rhi_properties.x < 0.0)
            smpCoord.y = 1.0 - smpCoord.y;

        vec2 uv = smpCoord.xy * uvAtlasScale + uvAtlasOffset;
        float storedDepth = textureLod(qt_shadowmap_texture, vec3(uv, layerIndex), 0.0).r;
        illumination = currentDepth < storedDepth ? 1.0 : 0.0;
    }

    return normalizedShadowResult(illumination, ubLights.lights[lightIndex].shadowFactor);
}

ShadowResult qt_sampleLightShadowMap(in int lightIndex, in vec3 worldPos)
{
    if (ubLights.lights[lightIndex].coneAngle > 180.0) {
        // Point
        return qt_samplePointLight(lightIndex, worldPos);
    } else {
        return qt_sampleSpotLight(lightIndex, worldPos);
    }
}

#endif
